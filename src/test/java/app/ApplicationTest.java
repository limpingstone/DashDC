package app;

import org.junit.Test;
import static org.junit.Assert.*;
import static org.junit.Assert.assertNull;
import org.junit.*;

import app.dashboard.*;
import app.asset.*;

import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
public class ApplicationTest {
    /*
    Application app = new Application();
    Dashboard dashboard; 
    @Before
    public void setup() {
	app.main(new String[] {});
	dashboard = app.dashboard;
    }

    // Tests the newPage() method
    // Artificially populates FormCapture 
    @Test
    public void testNewPage() {
	FormCapture form = new FormCapture();
	form.setId(1);
	form.setName("Page 1");
	app.newPage(form);
	app.pageOptions(form);
	
	assertEquals("Page 1", dashboard.getDashboardPage(1).getName());
    }

    // Tests the pageOptions() method
    // Artificially populates FormCapture
    
    @Test
    public void testPageOptions() {
	FormCapture form = new FormCapture();
	form.setId(2);
	form.setName("Page 2");
	app.newPage(form);
	app.pageOptions(form);
	    
	//assertNotNull(app.pageOptions());
    }
    
    */
}
